server:
  port: 8081

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ngatti68/repo-config-server.git
          searchPaths : config-data
          username : ${GIT_USER}
          password: ${GIT_PASSWORD}
